
document.cookie = "username=tatiana; SameSite=None; Secure";
console.log(document.cookie);
//let a = document.cookie;
//const cookies =  a.split(';').map(item => item.split('=')).reduce((acc, [k, v]) => (acc[k.trim().replace('"', '')] = v) && acc, {});
